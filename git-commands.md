1) git init: (Used to initializes a  Git repository)
2) git branch: (Used to see list of branches in repo)
3) git checkout <branch name>: (Used to switch branch)
4) git add .: (Used to adds all files changes to stagging area)
5) git commit -m "message": (Used to commit file changes present in stagging area)
6) addinf another command